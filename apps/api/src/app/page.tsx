import "@/lib/init"; // Initialize app on startup

export default function Home() {
	return (
		<main>
			<h1>Vijaya Bookstore API</h1>
			<p>Server is running. Use /api/v1/* endpoints.</p>
		</main>
	);
}
